<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <VersionPrefix>0.1.0</VersionPrefix>
    <TargetFrameworks>netstandard2.0;net472</TargetFrameworks>
    <FrameworkPathOverride Condition="'$(TargetFramework)' == 'net472' and '$(MONO_ROOT)' != ''">$(MONO_ROOT)/lib/mono/4.7.2-api/</FrameworkPathOverride>
    <Description>Provides platform-independent types for representing points, rectangles, and sizes.</Description>
    <PackageTags></PackageTags>
  </PropertyGroup>

  <ItemGroup>
  </ItemGroup>

</Project>
